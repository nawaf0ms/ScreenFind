# ScreenFind — تطبيق بحث نصي في الشاشة (Windows)

> وثيقة مواصفات تنفيذية موجّهة لوكيل برمجة. اقرأ القسم بالكامل قبل كتابة أي كود.

---

## 1. الملخص التنفيذي

أداة سطح مكتب لويندوز تتيح البحث عن نص داخل أي محتوى معروض على الشاشة **لا يدعم البحث أصلاً** — مثل ملفات PDF الممسوحة ضوئياً، الكتب المصوّرة، العارضات المحمية التي تمنع النسخ، جلسات Remote Desktop، والتطبيقات القديمة التي لا تحتوي على `Ctrl+F`.

**آلية العمل:** المستخدم يضغط اختصاراً عاماً → يظهر صندوق بحث عائم → التطبيق يلتقط الشاشة، يستخرج النص منها (عبر UI Automation أو OCR)، يطابقه مع كلمة البحث، ثم يرسم مستطيلات تظليل شفافة فوق النتائج على الشاشة مباشرة.

---

## 2. النطاق

### داخل النطاق (v1)
- البحث في **المحتوى المرئي على الشاشة فقط** (لا تمرير تلقائي، لا فهرسة للمستند كامل).
- دعم **العربية والإنجليزية بنفس درجة الأهمية**.
- تظليل بصري لجميع النتائج + تنقّل بينها.
- نسخ النص المُستخرَج إلى الحافظة.

### خارج النطاق (v1) — لا تنفّذها
- التمرير التلقائي أو فهرسة المستند كاملاً.
- استخراج الجداول أو الحفاظ على تنسيق المستند.
- منصات غير ويندوز.
- الترجمة أو التلخيص أو أي ذكاء اصطناعي توليدي.
- الوضع السحابي — **التطبيق يجب أن يعمل بالكامل أوفلاين**.

---

## 3. حزمة التقنيات

| المكوّن | التقنية | ملاحظة |
|---|---|---|
| المنصة | .NET 8 (`net8.0-windows10.0.19041.0`) | لازم لاستخدام WinRT APIs |
| الواجهة | WPF | نافذتان فقط: صندوق البحث + طبقة التظليل |
| الالتقاط | `Windows.Graphics.Capture` | سريع، بلا وميض، بلا حقوق مدير |
| النص الدقيق | `UI Automation` (`TextPattern`) | الطبقة الأولى، دقة 100% |
| OCR | `Windows.Media.Ocr` | مجاني، أوفلاين، مدمج في النظام |
| الصور | `System.Drawing` أو `SkiaSharp` | للمعالجة المسبقة قبل OCR |
| الاختصار العام | `RegisterHotKey` (P/Invoke) | |
| التغليف | single-file self-contained | |

> **قاعدة إلزامية:** كل تعامل مع OCR يمرّ عبر واجهة `IOcrEngine`. محرك ويندوز مجرد تنفيذ واحد لها. قد نحتاج استبداله بـ PaddleOCR أو Tesseract لاحقاً، ويجب ألا يكلّف الاستبدال أكثر من إضافة كلاس واحد.

---

## 4. المعمارية

```
Hotkey (Ctrl+Shift+F)
   │
   ├─► حفظ GetForegroundWindow()  ← قبل إظهار أي واجهة
   │
   ├─► إظهار SearchBar (topmost, no-activate)
   │
   └─► عند إدخال النص:
         │
         ├─► CaptureService ──► Bitmap
         │
         ├─► TextExtractionPipeline
         │      ├─ Tier 1: UiaTextExtractor   (نص + إحداثيات دقيقة)
         │      └─ Tier 2: OcrTextExtractor   (fallback)
         │           ├─ preprocess (×2 upscale, grayscale)
         │           ├─ OCR (ar)  ─┐
         │           └─ OCR (en)  ─┴─► merge + dedupe
         │
         ├─► ExtractedDocument { List<WordBox> }
         │
         ├─► TextNormalizer  ──► NormalizedText + IndexMap
         │
         ├─► MatchEngine (exact → fuzzy)  ──► List<Match>
         │
         └─► HighlightOverlay  ──► رسم المستطيلات
```

### نماذج البيانات الأساسية

```csharp
public record WordBox(
    string Text,
    Rect ScreenBounds,   // إحداثيات الشاشة الفعلية بعد تصحيح DPI
    string Language,     // "ar" | "en" | "unknown"
    float Confidence,
    ExtractionSource Source  // UIA | OCR
);

public record ExtractedDocument(
    IReadOnlyList<WordBox> Words,
    string RawText,          // نص متصل مبني من Words
    int[] WordStartOffsets   // فهرس بداية كل كلمة داخل RawText
);

public record Match(
    int StartWordIndex,
    int EndWordIndex,
    IReadOnlyList<Rect> Bounds,  // قد تكون عدة مستطيلات لو انقسم النص على سطرين
    float Score                   // 1.0 = مطابقة تامة
);
```

---

## 5. المكوّنات — تفاصيل التنفيذ

### 5.1 CaptureService

- استخدم `GraphicsCaptureItem.CreateFromWindowHandle` لالتقاط النافذة المستهدفة (المحفوظة قبل إظهار الواجهة)، لا الشاشة كاملة. هذا أدق ويتجنب التقاط واجهتك.
- إن فشل التقاط النافذة، ارجع إلى التقاط الشاشة كاملة عبر `CreateFromMonitorHandle`.
- **إلزامي:** استدعِ `SetWindowDisplayAffinity(hwnd, WDA_EXCLUDEFROMCAPTURE)` على كل نوافذ التطبيق (صندوق البحث + طبقة التظليل). بدون هذا، سيلتقط التطبيق واجهته الخاصة ويحاول قراءتها.
- خزّن hash للقطة (`xxHash` على البتات). إذا لم تتغيّر، أعد استخدام نتيجة الاستخراج السابقة بدل إعادة الـ OCR.

### 5.2 UiaTextExtractor (الطبقة الأولى)

```
AutomationElement.FromHandle(hwnd)
  → البحث عن عناصر تدعم TextPattern أو ValuePattern
  → TextPattern.DocumentRange.GetText()
  → TextPatternRange.GetBoundingRectangles() لكل نطاق
```

- إذا أعادت هذه الطبقة نصاً غير فارغ ومعقول، **تخطَّ الـ OCR تماماً**.
- معيار "معقول": أكثر من 20 حرفاً غير فراغ.
- كثير من التطبيقات (متصفحات، Word، تطبيقات WinUI) ستعمل هنا بدقة كاملة وسرعة أعلى بمراحل.
- لفّ كل شيء في `try/catch` — UIA يرمي استثناءات كثيرة وغير متوقعة مع التطبيقات القديمة.

### 5.3 OcrTextExtractor (الطبقة الثانية)

**المعالجة المسبقة (تؤثر على الدقة أكثر من أي شيء آخر):**
1. تكبير اللقطة ×2 بـ bicubic. نص الشاشة عند 96 DPI صغير جداً على محركات OCR المدرّبة على مستندات ممسوحة بـ 300 DPI.
2. تحويل إلى تدرّج رمادي.
3. **لا تطبّق binarization** إن كنت تستخدم `Windows.Media.Ocr` — نتائجه أسوأ معها. جرّبها فقط لو انتقلت إلى Tesseract.

**التعامل مع اللغتين:**
- `Windows.Media.Ocr` يدعم لغة واحدة لكل نسخة `OcrEngine`. أنشئ نسختين ثابتتين: `ar` و `en`.
- شغّل الاثنتين على نفس اللقطة (بالتوازي عبر `Task.WhenAll`).
- **الدمج:** لكل صندوق كلمة من النتيجتين، إن تجاوز التقاطع المكاني (IoU) 0.5 مع صندوق آخر، احتفظ بالأعلى ثقة فقط.
- تحقّق عند الإقلاع من `OcrEngine.AvailableRecognizerLanguages`. إن كانت العربية غير مثبتة، اعرض رسالة توجّه المستخدم إلى:
  `Settings → Time & Language → Language & region → Arabic → Options → Basic typing / OCR`

**تصحيح الإحداثيات:**
- إحداثيات الـ OCR نسبية للصورة المكبّرة ×2 → اقسم على معامل التكبير.
- ثم أضف إزاحة النافذة الملتقطة.
- ثم صحّح لمعامل DPI الخاص بالشاشة المعنية.
- اكتب اختبار وحدة لهذه الدالة تحديداً. أخطاء الإحداثيات هي أكثر مصدر للأخطاء في هذا النوع من التطبيقات.

### 5.4 TextNormalizer — حرج

يجب تطبيق **نفس الدالة بالضبط** على النص المستخرَج وعلى استعلام المستخدم.

```
1. حذف التشكيل:      U+064B .. U+0652, U+0670, U+0640 (تطويل)
2. توحيد الألف:      أ إ آ ٱ  →  ا
3. توحيد التاء:      ة  →  ه
4. توحيد الياء:      ى  →  ي
5. توحيد الهمزات:    ؤ → و  ،  ئ → ي  ،  ء → (حذف)
6. الأرقام:          ٠١٢٣٤٥٦٧٨٩ و ۰۱۲۳۴۵۶۷۸۹  →  0123456789
7. الإنجليزية:       ToLowerInvariant()
8. المسافات:         دمج المتتالية، حذف الأطراف
9. Unicode:          NormalizeString(NormalizationForm.FormKC)
```

> **الشرط الأهم:** أثناء التطبيع، ابنِ `IndexMap` — مصفوفة تربط كل موضع حرف في النص المطبَّع بموضعه في النص الأصلي. بدون هذه الخريطة، ستعثر على النتيجة لكن لن تعرف أي `WordBox` تظلّل. **لا تنفّذ التطبيع كسلسلة استبدالات بسيطة تفقد المواضع.**

### 5.5 MatchEngine

```
1. مطابقة حرفية على النص المطبَّع (IndexOf، كل التكرارات).
2. إن لم تُوجد نتائج → مطابقة ضبابية:
     - نافذة منزلقة بطول = طول الاستعلام ±30%
     - Levenshtein normalized ≥ 0.85
     - رتّب النتائج تنازلياً بالدرجة، خذ أعلى 50
3. حوّل مواضع الأحرف إلى WordBox عبر IndexMap + WordStartOffsets.
4. ادمج الصناديق المتجاورة على نفس السطر في مستطيل واحد.
5. لو امتدت النتيجة على سطرين، أعِد مستطيلين منفصلين.
```

المطابقة الضبابية ليست ترفاً: أخطاء الـ OCR مضمونة الحدوث (`ه↔ة`، `rn↔m`، `0↔O`، `l↔I`)، والبحث الحرفي وحده سيفشل باستمرار على المحتوى الممسوح ضوئياً.

### 5.6 HighlightOverlay

- نافذة WPF: `AllowsTransparency=True`, `WindowStyle=None`, `Topmost=True`, `ShowInTaskbar=False`, `Background=Transparent`.
- أضف `WS_EX_TRANSPARENT` عبر P/Invoke حتى تمرّ نقرات الفأرة إلى التطبيق الذي خلفها.
- ارسم كل النتائج بلون أصفر شفاف (`#80FFEB3B`)، والنتيجة النشطة بلون برتقالي (`#B0FF9800`) مع إطار.
- لا تحاول عكس اتجاه المستطيلات للنصوص العربية — الإحداثيات المُعادة من OCR/UIA بصرية أصلاً وصحيحة كما هي.

### 5.7 SearchBar

- شريط صغير عائم أعلى الشاشة، `Topmost`, `ShowActivated=False`.
- **إلزامي:** خزّن `GetForegroundWindow()` قبل عرضه. إظهار الصندوق يسرق التركيز، وبعدها لن تعرف أي نافذة كان المستخدم يقرأ فيها.
- بحث فوري أثناء الكتابة مع debounce 250ms.
- الاختصارات: `Enter` / `F3` = التالي، `Shift+Enter` = السابق، `Esc` = إغلاق كل شيء، `Ctrl+C` = نسخ النص المستخرَج كاملاً إلى الحافظة.
- اعرض عدّاد "3 / 17".

---

## 6. مطبّات ويندوز — لا تتجاهلها

| المطبّ | الحل |
|---|---|
| التطبيق يلتقط واجهته نفسه | `SetWindowDisplayAffinity(hwnd, WDA_EXCLUDEFROMCAPTURE)` |
| ضياع النافذة المستهدفة | احفظ `GetForegroundWindow()` **قبل** إظهار الواجهة |
| انحراف التظليل على شاشات مكبّرة | `<dpiAwareness>PerMonitorV2</dpiAwareness>` في app.manifest |
| بطء الـ OCR (0.5–2 ثانية) | كل شيء `async`، مؤشر تحميل، cache بـ hash اللقطة |
| دقة OCR ضعيفة | تكبير ×2 قبل التعرّف |
| تعدد الشاشات | إحداثيات موحّدة عبر `Screen.AllScreens`، طبقة تظليل لكل شاشة |
| استثناءات UIA العشوائية | `try/catch` حول كل استدعاء + timeout 2 ثانية |

---

## 7. خطة التنفيذ بالمراحل

### المرحلة 0 — اختبار الجدوى ⚠️ نفّذها أولاً، قبل أي كود إنتاجي
سكربت منفصل بسيط:
- التقط 20 لقطة شاشة حقيقية (PDF ممسوح عربي، PDF ممسوح إنجليزي، كتاب مصوّر، جلسة RDP).
- مرّرها على `Windows.Media.Ocr` باللغتين وقِس نسبة الكلمات الصحيحة.

**بوابة القرار:** إن كانت دقة العربية < 70%، **توقّف واستبدل المحرك** بـ PaddleOCR (عبر ONNX Runtime) أو Tesseract 5 مع `ara.traineddata`. هذا هو الخطر الوحيد القادر على إسقاط المشروع، ويجب حسمه في اليوم الأول.

### المرحلة 1 — النواة بلا واجهة (3–4 أيام)
اختصار عام → التقاط → OCR → طباعة الكلمات وإحداثياتها في الكونسول. **بدون أي واجهة رسومية.**
✅ **معيار القبول:** تطبع إحداثيات صحيحة لكلمات من PDF ممسوح مفتوح على الشاشة.

### المرحلة 2 — الواجهة والتظليل (3–4 أيام)
صندوق البحث + طبقة التظليل + التنقّل بين النتائج + نسخ النص للحافظة.
✅ **معيار القبول:** البحث عن كلمة إنجليزية يظلّلها في مكانها الصحيح بانحراف < 3 بكسل، على شاشة بتكبير 150%.

### المرحلة 3 — طبقة اللغة (2–3 أيام)
TextNormalizer + IndexMap + المطابقة الضبابية + دمج محركَي `ar`/`en`.
✅ **معيار القبول:** البحث عن «الطالبة» يجد «الطالبه»، والبحث عن «مدرسة» يجدها رغم وجود تشكيل في الأصل.

### المرحلة 4 — طبقة UI Automation (3 أيام)
`UiaTextExtractor` كطبقة أولى مع تخطّي الـ OCR عند نجاحها.
✅ **معيار القبول:** البحث داخل صفحة متصفح عادية يعطي دقة 100% وزمن استجابة < 200ms.

### المرحلة 5 — الصقل
تعدد الشاشات، cache، شاشة إعدادات (اختيار الاختصار، الألوان، اللغات المفعّلة)، مُثبِّت.

---

## 8. بنية المشروع المقترحة

```
ScreenFind/
├── ScreenFind.Core/
│   ├── Capture/       ICaptureService, GraphicsCaptureService
│   ├── Extraction/    ITextExtractor, UiaTextExtractor,
│   │                  OcrTextExtractor, IOcrEngine,
│   │                  WindowsOcrEngine, ExtractionPipeline
│   ├── Text/          TextNormalizer, IndexMap, MatchEngine, Levenshtein
│   ├── Models/        WordBox, ExtractedDocument, Match
│   └── Interop/       Win32.cs (كل الـ P/Invoke في مكان واحد)
├── ScreenFind.App/
│   ├── Windows/       SearchBarWindow, OverlayWindow
│   ├── Services/      HotkeyManager, AppOrchestrator
│   └── app.manifest   (PerMonitorV2)
└── ScreenFind.Tests/
    ├── NormalizerTests    ← تغطية شاملة لحالات العربية
    ├── IndexMapTests
    ├── MatchEngineTests
    └── CoordinateTests    ← تحويل DPI والإزاحات
```

---

## 9. أهداف الأداء

| العملية | الهدف | الحد الأقصى المقبول |
|---|---|---|
| ظهور صندوق البحث | < 100ms | 200ms |
| مسار UIA كامل | < 200ms | 400ms |
| مسار OCR كامل (شاشة 1080p) | < 1.2s | 2.5s |
| البحث في نص مُستخرَج مسبقاً | < 30ms | 100ms |
| استهلاك الذاكرة في الخمول | < 80MB | 150MB |

---

## 10. مبادئ عامة للوكيل

1. **ابنِ من الداخل إلى الخارج.** النواة أولاً مع كونسول، الواجهة لاحقاً. لا تبدأ بالـ UI.
2. **`IOcrEngine` قابل للاستبدال دائماً.** لا تسرّب أنواع `Windows.Media.Ocr` خارج `WindowsOcrEngine`.
3. **`ScreenFind.Core` يجب ألا يعتمد على WPF إطلاقاً**، ليبقى قابلاً للاختبار.
4. **كل الـ P/Invoke في `Interop/Win32.cs`** — لا تنثرها في الملفات.
5. **اكتب اختبارات لـ `TextNormalizer` و`IndexMap` وتحويل الإحداثيات قبل الاعتماد عليها.** هذه الثلاثة هي مصدر معظم الأخطاء الصامتة.
6. **كل عمليات الالتقاط والاستخراج `async`.** لا تجمّد واجهة المستخدم أبداً.
7. **صفر اتصال بالشبكة.** إن احتاج أي مكوّن إنترنت، فهو خيار خاطئ.
